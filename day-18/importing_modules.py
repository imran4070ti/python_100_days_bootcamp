import turtle  # Basic import / importing whole module
from turtle import Turtle  # from .... import ....
from turtle import *  # import everything from a module. Avoid if possible because it is confusing sometimes
import turtle as t  # import the module with some other name

# pip install <module_name>   -> to install a module pypi (python package index)
# pip install heroes
